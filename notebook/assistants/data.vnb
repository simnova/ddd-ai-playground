{cells:[{kind:2,language:'typescriptreact',value:'let openAiApiKey = import.meta.env.VITE_OPENAI_API_KEY;\n\nimport OpenAI from "openai";\nconst openai = new OpenAI({ apiKey: openAiApiKey});\n\nimport {data} from \'../../assistants/data\';\nimport {agent} from \'../../runners/agent\';\n\nconst messages = [\n    { role: "user" as const, \n    content: "Can you draft an email to sam altman thanking him from all the communities I manage and include a signature of my name" },\n];\n   \nconst result = await agent(openai,messages,data);\n\nVitale.markdown(result[0]?.content[0].text.value);\n\n',metadata:{id:'WTQ05LatLgV73nUWXLL5W'}}]}