{cells:[{kind:2,language:'typescript',value:'let openAiApiKey = import.meta.env.VITE_OPENAI_API_KEY;\n\nimport OpenAI from "openai";\nconst openai = new OpenAI({ apiKey: openAiApiKey});\n\nimport tools from \'../tools\';\nimport {conversation} from \'../conversation\';\n\nconst messages = [\n  { role: "user", content: "List me all the community names", name: "message-1" },\n];\n\nconst result = await conversation(openai,messages,tools);\n\nVitale.markdown(result[0].message.content);',metadata:{language:'typescript',id:'AvDnMnmezNyE5_1JsfV7a'}},{kind:2,language:'typescript',value:'const messages = [\n    { role: "user", content: "Which community has the value userIsAdmin = true?", name: "message-1" },\n  ];\n  \n  const result = await conversation(openai,messages,tools);\n  \n  Vitale.markdown(result[0].message.content);',metadata:{id:'Qw4hTfx0RsM75sTQG4Zv5'}}]}