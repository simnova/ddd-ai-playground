{cells:[{kind:2,language:'typescriptreact',value:'let openAiApiKey = import.meta.env.VITE_OPENAI_API_KEY;\n\nimport OpenAI from "openai";\nconst openai = new OpenAI({ apiKey: openAiApiKey});\n\nimport tools from \'../tools\';\nimport {conversation} from \'../runners/conversation\';\n\nconst messages = [\n  { role: "user", content: "Compose an email for me show it to me as a draft, it should be sent to samma@openai.com and should thank OpenAI for evolving function calling, include a proper subject and body, and make a nice signature for me (my name is Jane Doe). Return the raw json response from the tool. " },\n];\n\nconst result = await conversation(openai,messages,tools);\n\nVitale.markdown(result[0].message.content);',metadata:{id:'e_B1qlRelDx0K9O7ki3Y_'}}]}